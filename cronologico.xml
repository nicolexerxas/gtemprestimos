<odoo>
  <record id="ir_cron_overdue_installments" model="ir.cron">
    <field name="name">Marcar Parcelas Atrasadas</field>
    <field name="model_id" ref="model_gt_installment"/>
    <field name="state">code</field>
    <field name="code">
for parc in env['gt.installment'].search([('state','=','open'), ('due_date','<', fields.Date.context_today(env))]):
    parc.state = 'overdue'
    # opcional: marcar empréstimo como "bad" se X atrasos...
    env.cr.execute('UPDATE gt_loan SET state=%s WHERE id=%s', ('bad', parc.loan_id.id))
    </field>
    <field name="interval_number">1</field>
    <field name="interval_type">days</field>
    <field name="numbercall">-1</field>
  </record>
</odoo>